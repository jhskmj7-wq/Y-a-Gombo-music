RAPPORT D'AUDIT INTERNE (PHASE 1) :

1. **Boutons non fonctionnels / partiellement fonctionnels / simulés** :
   - `AdminWalletManagement.tsx` : 
     - L'ajustement du wallet (Créditer, Débiter, Fixer) fonctionne et a été renforcé avec l'Audit Log.
     - Bloquer / Geler fonctionne avec le PaymentEngine et un Audit Log.
     - Le Remboursement ("Rembourser") fonctionne avec le PaymentEngine et un Audit Log.
     - Les exportations CSV et JSON ("Export CSV", "Export JSON") étaient partiellement fonctionnelles (génération client, non tracée).
   - `AdminPollCenter.tsx` : Contient des actions simulées comme "Souverain (Simulation)" pour les votes.
   - `AdminAvatarStore.tsx` : Contient "Simulate AI asset generation architecture preparation".
   - `AdminUsers.tsx` : Indique "(Simulation active)" pour les pièces, selfies, preuves.

2. **Actions simulées identifiées** :
   - La génération d'assets AI dans l'avatar store.
   - Les votes de sondage depuis l'administrateur.
   - L'affichage de pièces/selfies/preuves dans `AdminUsers`.

3. **Paramètres / Données hardcodées** :
   - `AdminSettings.tsx` : Auparavant hardcodé, il est désormais connecté à `configs/pricing` (effectué lors de l'étape précédente).

4. **Mutations Firebase existantes** :
   - `transactions`
   - `walletTransactions`
   - `walletAdjustments`
   - `walletRefunds`
   - `users` (walletBalance, balance, walletBlocked, accountFrozen, etc.)
   - `configs` (pricing)
   - `admin_audit_logs` (créé pour tracer les actions sensibles)
   - `commissions`

5. **Cloud Functions** :
   - Le projet n'utilise pas de Cloud Functions. La logique est centralisée côté client avec Firebase Auth / Firestore (PaymentEngine, premiumEngine, etc.).

6. **Collections Firestore concernées** :
   - `users`
   - `transactions`
   - `walletTransactions`
   - `walletAdjustments`
   - `walletRefunds`
   - `configs`
   - `admin_audit_logs`
   - `commissions`

Actions immédiatement corrigées durant cette phase (Phase 2 & 11) :
- Ajout de la traçabilité (Audit Logs) lors d'opérations sensibles telles que : Ajustements de Wallet, Remboursements de transactions, Blocages et Gels de comptes.
